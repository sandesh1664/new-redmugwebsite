import type { MetadataRoute } from "next";
import { eq } from "drizzle-orm";
import { db } from "@/db";
import { blogPosts, careers, pages, projects, services } from "@/db/schema";
export const dynamic="force-dynamic";
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL||"https://redmugitsolution.ae";const[serviceRows,projectRows,postRows,careerRows,pageRows]=await Promise.all([db.select({slug:services.slug,updatedAt:services.updatedAt}).from(services).where(eq(services.status,"PUBLISHED")),db.select({slug:projects.slug,updatedAt:projects.updatedAt}).from(projects).where(eq(projects.status,"PUBLISHED")),db.select({slug:blogPosts.slug,updatedAt:blogPosts.updatedAt}).from(blogPosts).where(eq(blogPosts.status,"PUBLISHED")),db.select({slug:careers.slug,updatedAt:careers.updatedAt}).from(careers).where(eq(careers.status,"OPEN")),db.select({slug:pages.slug,updatedAt:pages.updatedAt}).from(pages).where(eq(pages.status,"PUBLISHED"))]);const fixed=["","about","services","solutions","industries","projects","blog","careers","contact"].map(path=>({url:`${base}/${path}`,lastModified:new Date(),changeFrequency:"weekly" as const,priority:path?0.7:1}));return[...fixed,...pageRows.filter(page=>page.slug!=="home"&&!fixed.some(item=>item.url.endsWith(`/${page.slug}`))).map(page=>({url:`${base}/${page.slug}`,lastModified:page.updatedAt,changeFrequency:"monthly" as const,priority:.6})),...serviceRows.map(row=>({url:`${base}/services/${row.slug}`,lastModified:row.updatedAt,changeFrequency:"monthly" as const,priority:.8})),...projectRows.map(row=>({url:`${base}/projects/${row.slug}`,lastModified:row.updatedAt,changeFrequency:"monthly" as const,priority:.7})),...postRows.map(row=>({url:`${base}/blog/${row.slug}`,lastModified:row.updatedAt,changeFrequency:"monthly" as const,priority:.6})),...careerRows.map(row=>({url:`${base}/careers/${row.slug}`,lastModified:row.updatedAt,changeFrequency:"weekly" as const,priority:.6}))]}
